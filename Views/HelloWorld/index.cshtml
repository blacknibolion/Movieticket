@{
    ViewData["Title"]= "Movie List";
}

<h2>Movie List</h2>

<p>a simple message from us</p>

<ul>
    @for (int i = 0; i < (int)ViewData["NumTimes"]!; i++)
    {
        <li>@ViewData["Message"]</li>
    }
</ul>